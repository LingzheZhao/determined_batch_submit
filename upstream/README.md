# Upstream references

The original repository kept Determined's source as git submodules under `src/`.
Those bits are not part of the distributable package but can be checked out here
when you need to compare behaviour with upstream Determined releases.
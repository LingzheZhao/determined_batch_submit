interface:
  display_name: "Intensive Compute Runner"
  short_description: "Run heavy work through the repository compute service."
  default_prompt: "Use $intensive-compute-runner to plan and operate this heavy task through the repository's Determined compute service."

policy:
  allow_implicit_invocation: true
